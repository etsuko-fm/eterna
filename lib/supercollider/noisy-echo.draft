/*
(
{
	var env= EnvGen.ar(Env.perc());
	var in = env * PinkNoise.ar(0.1);
    var delayTime = 0.2 + LFNoise2.kr(0.2).range(-0.03, 0.03); // slow mod
    var fb = LocalIn.ar(1);
    var sig = in + fb;
    sig = DelayL.ar(sig, 1.0, delayTime);
	sig = BPF.ar((sig * 1.5).tanh * 1/1.5);
	LocalOut.ar(CombL.ar(sig*0.9));
    sig
}.play;
)
*/